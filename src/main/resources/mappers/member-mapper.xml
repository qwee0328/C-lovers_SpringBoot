<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="member">
  	<select id="login" resultType="Boolean">
  		select count(*) from employee where id=#{id} and pw=#{pw}
  	</select>
  	
  	<update id="updatePW">
  		update employee set pw=#{pw} where id=#{id}
  	</update>
  	
  	<select id="isManager" resultType="List">
  		select authority_category from admin where emp_id=#{id}
  	</select>
  </mapper>